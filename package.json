{
  "name": "playwright_tuts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tests:api": "playwright test --config=api.config.js --project=Chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "playwright": "^1.37.1"
  }
}
